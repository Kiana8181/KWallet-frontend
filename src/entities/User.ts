export interface User {
  // ID: number;
  // CreatedAt: string;
  // UpdatedAt: string;
  // DeletedAt: string | null;
  username: string;
  walletId: string;
  email: string;
  firstname: string;
  lastname: string;
  password: string;
}
